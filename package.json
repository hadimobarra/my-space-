{
  "name": "my-portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/landing",
    "build": "npm run build --workspaces",
    "build:site": "bash scripts/build.sh",
    "lint": "npm run lint --workspaces"
  }
}
